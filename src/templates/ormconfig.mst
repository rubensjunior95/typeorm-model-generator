[
  {
    "name": "default",
    "type": "{{databaseType}}",
    "host": "{{host}}",
    "port": {{port}},
    "username": "{{user}}",
    "password": "{{password}}",
    "database": "{{databaseNames}}",{{#schemaName}}
    "schema": "{{.}}",{{/schemaName}}{{#instanceName}}
    "extra": { "instanceName": "{{.}}" },{{/instanceName}}
    "synchronize": false,
    "entities": [
      "entities/*.js"
    ]
  }
]
